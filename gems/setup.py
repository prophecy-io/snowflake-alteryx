
from setuptools import setup, find_packages
setup(
    name = 'AlteryxSnowflake',
    version = '0.0.1',
    packages = ['AlteryxSnowflake'],
    package_dir = {'AlteryxSnowflake': '.'},
    description = '',
    install_requires = [],
)

